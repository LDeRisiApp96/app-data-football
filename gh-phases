# Vai nella directory del progetto
cd app-data-football

# Crea un nuovo branch orfano (senza la storia del codice)
git checkout --orphan gh-pages

# Rimuovi tutti i file dal staging
git rm -rf .

# Crea una cartella docs per il sito
mkdir docs
cd docs

# Copia il README nella cartella docs
cp ../README.md .

# Torna alla radice
cd ..
